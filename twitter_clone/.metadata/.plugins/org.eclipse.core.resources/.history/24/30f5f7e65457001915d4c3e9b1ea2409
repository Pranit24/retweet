package com.me.dao;

import java.util.List;
import java.util.Set;

import org.hibernate.Criteria;
import org.hibernate.HibernateException;
import org.hibernate.criterion.Restrictions;
import org.hibernate.query.Query;

import com.me.pojo.Tweet;
import com.me.pojo.User;

public class TweetDao extends DAO{

//	public boolean addTweet(Tweet message, User user) {
//		boolean saved = false;
//		try {
//			begin();
//			user.setListOfTweets(message);
//			for(Tweet msg: user.getListOfTweets()) {
//				System.out.println(msg.getMessage());
//				System.out.println("----");
//			}
//			getSession().update(user);
//			commit();
//			
//		}catch (HibernateException e) {
//			rollback();
//		}finally {
//			close();
//		}
//		
//		return saved;
//	}
	
	public void addTweet(User user) {
		
		try {
			begin();
			Criteria criteria = getSession().createCriteria(Tweet.class);
			criteria.add(Restrictions.eq("USERID", user.getUserId()));
			
			List<Tweet> tw = criteria.list();
			System.out.println(tw.size());
			user.setListOfTweets(tw);
			getSession().update(user);
			commit();
			
		}catch (HibernateException e) {
			rollback();
		}finally {
			close();
		}
	}
}
